<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.ccci</groupId>
  <artifactId>obiee-ws-client</artifactId>
  <name>OBIEE Web Service Client</name>
  <version>1-SNAPSHOT</version>
  <description>A client library generated from the wsdl of CCCI's OBIEE instance </description>
  
  <parent>
    <groupId>org.ccci</groupId>
    <artifactId>abstract-java-project</artifactId>
    <version>2</version>
    <relativePath/>
  </parent>
 
  <ciManagement>
    <system>Bamboo</system>
  </ciManagement>
  
  <scm>
    <connection>scm:svn:http://arkham.ccci.org/svn/java/trunk/commons/obiee-ws-client/</connection>
    <developerConnection>scm:svn:http://arkham.ccci.org/svn/java/trunk/commons/obiee-ws-client/</developerConnection>
  </scm>
  
  <build>
    <plugins>
      <plugin>
        <groupId>org.jvnet.jax-ws-commons</groupId>
        <artifactId>jaxws-maven-plugin</artifactId>
        <version>2.2</version>
        <executions>
          <execution>
            <goals>
              <goal>wsimport</goal>
            </goals>
            <configuration>
              <target>2.1</target>
              <wsdlDirectory>src/main/resources/wsdl</wsdlDirectory>
              <wsdlFiles>
                <!-- This is a slightly modified copy of https://insightstage.ccci.org/Analytics/saw.dll?WSDL
                It has been modified in two ways:
                 1)  the endpoint urls have been changed to 'https' instead of 'http'; the jax-ws client can't handle
                   the loadbalancer's 302 redirect from http to https urls, and all of the links in the Answers-published wsdl 
                   currently are http urls (it doesn't realize it has an https proxy in front, because WebLogic hasn't been configured
                   accordingly)
                 2) the <wsdl:import> element has been replaced by a <wsdl:types> element with an <xsd:import>.  Not sure
                   why the published Answers wsdl screws this up.
                  -->
                <wsdlFile>siebel-analytics-web.wsdl</wsdlFile>
              </wsdlFiles>
                <!-- 
                This isn't actually useful; there isn't a classpath URL handler.  But specifying 
                https://insightstage.ccci.org/Analytics/saw.dll?WSDL isn't helpful, really, because that
                published wsdl is unusable by jax-ws for the reasons mentioned above.
                -->
              <wsdlLcation>classpath:/wsdl/siebel-analytics-web.wsdl</wsdlLcation>
              <args>
               <param>-b</param>
               <param>src/main/resources/jaxws-bindings.xml</param>
               <param>-b</param>
               <param>src/main/resources/jaxb-bindings.xml</param>
              </args>
            </configuration>
          </execution>
        </executions>

      </plugin>
      
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-sources</id>
            <phase>verify</phase>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
     </plugin>
     
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>1.7</version>
        <executions>
          <execution>
            <id>add-source</id>
            <phase>validate</phase>
            <goals>
              <goal>add-source</goal>
            </goals>
            <configuration>
              <sources>
                <source>${basedir}/target/generated-sources/wsimport</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>  
    </plugins>
   </build> 
   
  <dependencies>
    <dependency>
      <groupId>org.testng</groupId>
      <artifactId>testng</artifactId>
       <version>5.10</version>
      <classifier>jdk15</classifier>
      <scope>compile</scope>
      <exclusions>
        <exclusion>
          <groupId>junit</groupId> 
          <artifactId>junit</artifactId>
        </exclusion>
      </exclusions>
    </dependency>


    <dependency>
      <groupId>javax.xml.ws</groupId>
      <artifactId>jaxws-api</artifactId>
      <version>2.1-1</version>
      <scope>provided</scope>
      
      <exclusions>
        <exclusion>
          <groupId>javax.jws</groupId> 
          <artifactId>jsr181</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>javax.jws</groupId>
      <artifactId>jsr181-api</artifactId>
      <version>1.0-MR1</version>
      <scope>provided</scope>
    </dependency>
  
    <dependency>
      <groupId>com.sun.xml.ws</groupId>
      <artifactId>jaxws-rt</artifactId>
      <version>2.1.4</version>
      <scope>provided</scope>
    </dependency>
  
    <dependency>
      <groupId>commons-pool</groupId>
      <artifactId>commons-pool</artifactId>
      <version>1.5.5</version>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
      <version>1.2.14</version>
    </dependency>

  </dependencies>
  
  <properties>
  <!-- 
    <http.nonProxyHosts>hart-*|harta*|*.ccci.org</http.nonProxyHosts>
   -->
  </properties>
</project>